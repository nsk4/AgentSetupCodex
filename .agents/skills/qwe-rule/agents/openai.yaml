interface:
  display_name: "QWE Rule"
  short_description: "Capture a persistent QWE rule"
  default_prompt: "Use $qwe-rule to capture this correction or preference as a persistent rule."

policy:
  allow_implicit_invocation: false
