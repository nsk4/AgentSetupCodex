interface:
  display_name: "QWE Linear"
  short_description: "Create a concise Linear issue"
  default_prompt: "Use $qwe-linear to create a Linear issue from this request."

policy:
  allow_implicit_invocation: false
