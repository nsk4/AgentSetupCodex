# My setup

Describe the repositories you commonly work in and the labels the qwe workflow should use for them.

I build `<repo label and purpose>` and `<repo label and purpose>`, usually as separate repos. When I say `<label>`, I mean `<repo/path or identifying rule>`. These are the repo labels the qwe workflow scopes to — a repo label in a qwe prompt resolves through this mapping.

All the actual per-repo detail — naming conventions, folder layout, how to work in that repo, its skills and agents — lives IN each repo (its instructions / rules / skills). Follow those; don't assume conventions from here.
