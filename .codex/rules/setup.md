# My setup

Describe the repositories in this project and the labels the QWE workflow should use for them.

I build `<repo label and purpose>` and `<repo label and purpose>`, usually as separate repos. When I say `<label>`, I mean `<repo/path or identifying rule>`. A repo label in a QWE request resolves through this mapping.

All other per-repo detail — naming conventions, folder layout, how to work in that repo, its skills and agents — lives in the applicable project or repository instructions, rules, and skills. Follow those; don't assume conventions from here.
