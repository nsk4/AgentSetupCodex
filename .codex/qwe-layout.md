# qwe layout — machine mode (terminal fallback; paths relative to ~/.codex/).
# When this repo is deployed as a product's .codex/, the product root's qwe-layout.md overrides this.
mode: machine
plans: plans
rules: rules
logs: logs
templates: templates
